{
  "name": "venture-sandbox-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "mcp-server",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run start --workspace=backend\" \"npm run dev --workspace=frontend\" \"npm run start --workspace=mcp-server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
